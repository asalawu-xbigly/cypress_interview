{
  "devDependencies": {
    "cypress": "^13.4.0",
    "cypress-mochawesome-reporter": "^3.7.0",
    "cypress-otp": "^1.0.3",
    "cypress-plugin-tab": "^1.0.5"
  },
  "dependencies": {
    "@aws-sdk/client-ssm": "^3.485.0",
    "cucumber": "^6.0.7",
    "cypress-cucumber-preprocessor": "^4.3.1",
    "cypress-real-events": "^1.10.3",
    "minimatch": "^9.0.3",
    "moment": "^2.29.4",
    "pg": "^8.11.3",
    "preprocessor": "^1.3.0",
    "prettier": "^3.0.3",
    "south-african-fake-id-generator": "^1.0.16",
    "totp-generator-ts": "^1.0.3"
  },
  "scripts": {
    "cypress:open": "cypress open",
    "cy:run": "npx cypress run",
    "cy:regressiontest": "npx cypress run --browser chrome -e TAGS='@regression'",
    "cy:development-regressiontest": "npx cypress run --browser chrome -e TAGS='@regression' --config-file config.development.js",
    "cy:stage-regressiontest": "npx cypress run --browser chrome -e TAGS='@regression' --config-file config.staging.js",
    "cy:smoketest": "npx cypress run --browser chrome -e TAGS='@smoke'"
  },
  "cypress-cucumber-preprocessor": {
    "nonGlobalStepDefinitions": false,
    "step_definitions": "cypress/e2e/step_definitions"
  }
}
